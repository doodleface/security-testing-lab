# Python Framework Misconfiguration Lab

SecurityTestingLab runtime target app for Flask/Django-style route, template, upload, debug-header, and auth/session misconfiguration surfaces beyond Django.nV and Vulpy.

This source cache is generated for the authorized internal lab and is pinned by local git revision.
