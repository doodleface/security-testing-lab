# CORS JWT Header Misconfiguration Lab

SecurityTestingLab runtime target app for Deterministic CORS, JWT, CSP, HSTS, security-header, and auth-boundary misconfiguration variants.

This source cache is generated for the authorized internal lab and is pinned by local git revision.
