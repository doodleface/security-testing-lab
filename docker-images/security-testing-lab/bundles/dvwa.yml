services:
  dvwa:
    image: securitytestinglab/dvwa-target:local
    container_name: securitytestinglab-dvwa
    restart: unless-stopped
    ports:
    - ${DVWA_BIND_HOST:-0.0.0.0}:${DVWA_PORT:-8080}:80
    build:
      context: ./dvwa
